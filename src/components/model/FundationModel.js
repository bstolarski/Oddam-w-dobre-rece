export default class FundationModel{
    constructor(id, name, mission, stuff, page) {
        this.id = id;
        this.name = name;
        this.mission = mission;
        this.stuff = stuff;
        this.page = page;
    }
}